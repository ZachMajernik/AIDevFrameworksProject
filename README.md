-- The directions and structure portion of this readme were generated by copilot --

# AIDevFrameworksProject

This project is a small full-stack CRUD application with an Express frontend, a FastAPI backend, and a MongoDB database. The app lets users create, view, update, and delete items through a browser UI while the backend exposes the API routes and persists data in MongoDB.

## Database Choice

This project uses MongoDB. I chose MongoDB because the item data is naturally document-shaped, the schema is simple, and it fits well with a lightweight CRUD application where each item can be stored as a single JSON-like document.

## Docker Setup

The project is configured to run with Docker Compose.

### Start the application

From the project root, run:

```bash
docker-compose up --build
```

If your Docker installation uses the newer Compose plugin, this also works:

```bash
docker compose up --build
```

### What starts

- `front_end` on port `3000`
- `back_end` on port `8000`
- `db` using MongoDB with a persistent Docker volume

After startup:

- Frontend: `http://localhost:3000`
- Backend API: `http://localhost:8000`

## Architecture

The system is split into three parts:

- The Express frontend renders EJS views and handles browser requests.
- The FastAPI backend handles the CRUD API and business logic.
- MongoDB stores the item documents persistently.

### ASCII diagram

```text
Browser
	|
	v
Express Frontend (front_end, port 3000)
	|
	| HTTP requests to API_BASE_URL
	v
FastAPI Backend (back_end, port 8000)
	|
	| MongoDB queries through DAL.py
	v
MongoDB (db, Docker volume: db_data)
```

## Project Structure

```text
front_end/
  front_end.js       Express app and routes
  views/             EJS templates
  public/            Static assets

back_end/
  main.py            FastAPI routes
  DAL.py             MongoDB data access layer

docker-compose.yml   Multi-container app setup
requirements.txt     Python dependencies
```

## Endpoints

The backend currently exposes these routes:

- `GET /items` - return all items
- `GET /item/{id}` - return one item by id
- `POST /new-item` - create a new item
- `PUT /update-item/{id}` - update an existing item
- `DELETE /delete-item/{id}` - delete an item